<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <title></title>
</head>
<body>
    
    <% allCategory.map(e => { %>
      <div class="nat_box">
        <div class="nat_card">
          <img class="nat_img" src="<%= e.logo %>" alt="logo" width="200" height="200" />
          <a class="nat_text" href="<%= e.name %>"><%= e.name %></a>
        </div>

      </div>
        <%})%>
</body>
</html>